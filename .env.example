# Configuración inyectada en tiempo de build por GitHub Actions
# No uses VITE_* — la config se inyecta via config.js + secrets
#
# Para desarrollo local:
# 1. Copia config.js a public/config.js
# 2. Reemplaza los placeholders con tus credenciales reales
#
# window.__APP_CONFIG__ = {
#   supabaseUrl: 'https://tu-proyecto.supabase.co',
#   supabaseKey: 'tu-anon-key',
#   whatsappNumber: '5350979465',
# };
